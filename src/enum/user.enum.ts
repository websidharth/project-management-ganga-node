export enum Role {
  SUPER_ADMIN = 'SUPER_ADMIN',
  ADMIN = 'ADMIN',
  USER = 'USER',
  STAFF = 'STAFF',
  // Keep legacy values for backwards compatibility
  User = 'USER',
  Administrator = 'Administrator',
}